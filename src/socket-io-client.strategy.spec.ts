import { SocketIoClientStrategy } from './socket-io-client.strategy';

describe('SocketIoClientStrategy', () => {
  it('should be defined', () => {
    expect(new SocketIoClientStrategy()).toBeDefined();
  });
});
